import type { RunAndDebugState } from '../RunAndDebugState/RunAndDebugState.ts'

export const handleWatchExpressionDoubleClick = async (state: RunAndDebugState, dataIndex: string): Promise<RunAndDebugState> => {
  return state
}
